# Available Tools

## Domain tools
- `route_email_payload(payload: str)` — route an email intelligence payload
- `get_routing_history(last_n: int)` — get recent routing decisions

## Platform tools
- `send_message` — send A2A messages to other agents
- `memory_search` — search workspace memory
- `daily_log` — log daily activity
